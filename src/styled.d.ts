import "styled-components";

declare module "styled-components" {
  export interface DefaultTheme {
    bgColor: string;
    btnBgColor: string;
    btnPickBgColor: string;
    lineColor: string;
    textColor: string;
    textColor2: string;
    accentColor: string;
  }
}
